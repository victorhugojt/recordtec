# Monitoring VM Environment (optional)
# The otel-lgtm container doesn't require env vars, but you can configure:

# Grafana admin password (change after first login)
GF_SECURITY_ADMIN_PASSWORD=admin

# Data retention
GF_PATHS_DATA=/data/grafana

# Optional: Configure alert notifications, SMTP, etc.
